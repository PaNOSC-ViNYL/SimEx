INCLUDE(ExternalProject)

file(MAKE_DIRECTORY "${CMAKE_INSTALL_PREFIX}/lib/python")

ExternalProject_Add(
    wpg
    DOWNLOAD_COMMAND ""
    SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}
    CONFIGURE_COMMAND ""
    CMAKE_COMMAND ""
    MAKE_COMMAND ""
    BUILD_COMMAND ""
    BUILD_IN_SOURCE 1
    INSTALL_COMMAND PYPATH=${PYPATH} PACKAGES=${PACKAGES} PATCHES=${PATCHES} SRC=${MODULES_SCR} make -f wpg.mk wpg
    )
    
