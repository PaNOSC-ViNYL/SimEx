INCLUDE(ExternalProject)

file(MAKE_DIRECTORY "${FRAMEWORK_DIR}/unittest/TestFiles/")
file(MAKE_DIRECTORY "${FRAMEWORK_DIR}/src/SimEx/Utilities/")

ExternalProject_Add(
    s2e_files
    DOWNLOAD_COMMAND ""
    SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}
    CONFIGURE_COMMAND ""
    CMAKE_COMMAND ""
    MAKE_COMMAND ""
    BUILD_COMMAND ""
    BUILD_IN_SOURCE 1
    INSTALL_COMMAND SIMEX=${FRAMEWORK_DIR} PYPATH=${PYPATH} PACKAGES=${PACKAGES} PATCHES=${PATCHES} SRC=${MODULES_SCR} make -f s2e_files.mk s2e_files
    )
    
