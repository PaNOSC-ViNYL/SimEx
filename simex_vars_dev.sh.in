#!/bin/sh

export PATH=${CMAKE_INSTALL_PREFIX}/${BINDIR}:$PATH
export PYTHONPATH=${SIMEX_SOURCE_DIR}/${TESTSDIR}/python/unittest:${FRAMEWORK_DIR}/python:${CMAKE_INSTALL_PREFIX}/${PYPATH}:$PYTHONPATH
export SIMEX_TESTS=${SIMEX_SOURCE_DIR}/${TESTSDIR}
